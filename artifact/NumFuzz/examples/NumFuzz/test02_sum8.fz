#include "nlibrary.fz"

function test02_sum8
  (x0 : num) (x1 : num) (x2 : num) (x3 : num)
  (x4 : num) (x5 : num) (x6 : num) (x7 : num) 
{
  sa = addfp (|x0,x1|);
  let a = sa;
  sb = addfp (|a,x2|);
  let b = sb;
  sc = addfp (|b,x3|);
  let c = sc;
  sd = addfp (|c,x4|);
  let d = sd;
  se = addfp (|d,x5|);
  let e = se;
  sf = addfp (|e,x6|);
  let f = sf;
  addfp (|f,x7|)
}

test02_sum8
