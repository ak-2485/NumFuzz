function FMA (x : num) (y : num) (z : num) {
  a = mul(x,y);
  b = add(|a,z|);
  rnd b
}

function Horner2
  (a0 : num) (a1 : num) (a2 : num) (x : ![2.0]num) {
  let [x'] = x ;
  z = ((FMA a2) x') a1;
  let  y = z;
  ((FMA y) x') a0
}

Horner2
